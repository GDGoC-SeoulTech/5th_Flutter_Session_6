name: pokemon
description: "GDGoC 5th App Session"
publish_to: "none"
version: 0.1.0

environment:
    sdk: ^3.9.2

dependencies:
    cupertino_icons: ^1.0.8
    flutter:
        sdk: flutter
    http: ^1.5.0

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^5.0.0

flutter:
    uses-material-design: false
